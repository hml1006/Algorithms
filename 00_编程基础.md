# 第 00 章：编程基础与语言工具

> **编程基础是算法学习的起点。** 无论你选择哪种编程语言，扎实的编程基础、对语言特性的熟练掌握、高效的输入输出与调试技巧，以及时间复杂度的分析能力，都是学好算法与数据结构的必备前提。

本章将围绕编程语言选择、必备语言特性、输入输出与调试技巧、时间复杂度分析四大主题展开，帮助你在算法学习上打下坚实的基础。

---

## 2.1 编程语言选择

### 2.1.1 为什么要关注语言选择？

在算法学习和竞赛中，编程语言的选择直接影响到编码效率、代码可读性和程序运行性能。不同的语言有不同的设计哲学、标准库和生态，适合不同的场景。

- **算法竞赛（OI/ACM）**：追求极致性能，C++ 是绝对主流。Python 在部分赛题中也有使用，但受限于性能。
- **LeetCode / 面试**：Python 和 Java 使用最广，C++ 紧随其后，Rust 正在兴起。
- **工程实践**：Java 和 Rust 在工程领域表现出色，C++ 在底层系统和高性能计算中占据主导。

### 2.1.2 各语言对比

| 语言 | 竞赛适用性 | 面试适用性 | 运行速度 | 开发效率 | 学习曲线 | 主要优势 |
|------|:---------:|:---------:|:--------:|:--------:|:--------:|---------|
| **C++** | ⭐⭐⭐ (首选) | ⭐⭐⭐ | 极快 | 中等 | 较陡 | STL 丰富，性能最优，竞赛标配，接近硬件 |
| **Python** | ⭐⭐ | ⭐⭐⭐ | 较慢 | 极高 | 平缓 | 语法简洁，快速原型，库丰富，适合面试 |
| **Java** | ⭐⭐ | ⭐⭐⭐ | 较快 | 中等 | 中等 | 面向对象，工程性强，大数据生态成熟 |
| **Rust** | ⭐ | ⭐⭐ | 极快 | 中等 | 较陡 | 内存安全，零成本抽象，现代语言特性 |

#### C++

C++ 是算法竞赛的事实标准语言。它提供了 STL（Standard Template Library，标准模板库），包含 vector、set、map、unordered_map、queue、stack、priority_queue、algorithm、bitset、string 等强大的容器和算法。C++ 的优点在于**性能极优**——可以精确控制内存布局和数据结构的底层实现，且编译时优化使得运行速度接近硬件极限。在 OI 和 ACM 竞赛中，约 90% 以上的选手使用 C++。

#### Python

Python 因其**简洁优雅的语法**和**极高的开发效率**，成为 LeetCode 面试中最受欢迎的语言。Python 的代码量通常只有 C++ 或 Java 的 1/3 到 1/2。它的标准库和第三方库（如 collections、itertools、math、functools、bisect、heapq）为算法实现提供了极大的便利。但 Python 的运行速度较慢，在数据规模极大的竞赛题目中可能会超时。

#### Java

Java 在大型企业级应用中占据主导地位，也是 LeetCode 官方支持的三大语言之一。Java 的 java.util.* 包提供了丰富的集合类（ArrayList、HashMap、TreeSet、PriorityQueue 等），java.math.* 包支持高精度运算。Java 的**强类型和面向对象特性**使其代码结构清晰，适合大型项目，但相比 C++ 和 Python，编写算法代码时略显冗长。

#### Rust

Rust 是一门**内存安全**的现代系统编程语言，近年来在算法社区中逐渐获得关注。它的所有权系统在编译期消除了空指针、数据竞争等常见内存错误，同时保持了与 C++ 相当的性能。Rust 的标准库提供了 Vec、HashMap、HashSet、BinaryHeap 等常用数据结构。Rust 的**学习曲线较陡**，但一旦掌握，能写出非常安全且高效的算法代码。

### 2.1.3 综合建议

| 场景 | 推荐语言 | 理由 |
|------|---------|------|
| OI / ACM 竞赛 | C++ | 性能最优，STL 强大，社区资源丰富 |
| LeetCode 面试 | Python 或 C++ | Python 开发快，C++ 性能好 |
| 工程实践 | Java 或 Rust | 类型安全，生态成熟，适合大规模系统 |
| 初学者入门 | Python | 语法简单，易上手，快速获得正反馈 |

### 本节小结

| 关键点 | 说明 |
|--------|------|
| C++ 是竞赛主流 | 性能最优，STL 丰富，适合 OI/ACM |
| Python 适合面试 | 开发效率高，语法简洁，适合快速实现 |
| Java 工程性强 | 面向对象，生态成熟，适合大型项目 |
| Rust 安全现代 | 内存安全，零成本抽象，学习曲线较陡 |
| 初学者推荐 Python | 上手快，正反馈强，适合建立编程思维 |

---

## 2.2 必备语言特性

### 2.2.1 为什么需要掌握语言特性？

算法竞赛和面试中，**对语言标准库的熟练掌握程度**直接影响解题速度和代码质量。高效使用内置容器和算法，可以让你专注于问题本身的逻辑，而不是重复造轮子。

### 2.2.2 C++ 必备特性

C++ 在算法竞赛中最重要的就是 STL，以下是最常用的组件：

| 容器/工具 | 头文件 | 说明 | 时间复杂度 |
|-----------|--------|------|:----------:|
| `vector` | `<vector>` | 动态数组，支持随机访问 | 随机访问 O(1)，尾部插入 O(1) 均摊 |
| `set` | `<set>` | 有序集合（红黑树实现） | 插入/删除/查找 O(log n) |
| `map` | `<map>` | 有序键值对（红黑树实现） | 插入/删除/查找 O(log n) |
| `unordered_set` | `<unordered_set>` | 哈希集合 | 插入/删除/查找 O(1) 均摊 |
| `unordered_map` | `<unordered_map>` | 哈希映射 | 插入/删除/查找 O(1) 均摊 |
| `queue` | `<queue>` | 队列（FIFO） | push/pop O(1) |
| `stack` | `<stack>` | 栈（LIFO） | push/pop O(1) |
| `priority_queue` | `<queue>` | 优先队列（堆） | push/pop O(log n)，top O(1) |
| `deque` | `<deque>` | 双端队列 | 两端插入/删除 O(1) 均摊 |
| `bitset` | `<bitset>` | 位集，支持位运算 | 位运算 O(n/wordsize) |
| `string` | `<string>` | 字符串 | 支持拼接、查找、子串等 |
| `algorithm` | `<algorithm>` | 排序、二分查找、排列等 | sort O(n log n)，lower_bound O(log n) |
| `functional` | `<functional>` | 函数对象、lambda | 用于自定义比较器 |

**常用 `algorithm` 函数**：

- `sort(first, last)`：快速排序，平均 O(n log n)
- `stable_sort(first, last)`：稳定排序
- `lower_bound(first, last, val)` / `upper_bound(first, last, val)`：二分查找，要求有序，O(log n)
- `binary_search(first, last, val)`：判断是否存在，O(log n)
- `reverse(first, last)`：反转区间
- `unique(first, last)`：去重（相邻重复），返回新末尾
- `next_permutation(first, last)` / `prev_permutation(first, last)`：下一个/上一个排列
- `max_element(first, last)` / `min_element(first, last)`：求最大/最小值位置
- `accumulate(first, last, init)`：区间求和
- `fill(first, last, val)`：区间赋值

### 2.2.3 Python 必备特性

Python 的标准库非常强大，以下是最常用的模块和工具：

| 模块 | 常用功能 | 说明 |
|------|---------|------|
| `collections` | `deque`, `Counter`, `defaultdict`, `OrderedDict` | 双端队列、计数器、默认字典、有序字典 |
| `itertools` | `permutations`, `combinations`, `product`, `chain` | 排列组合、笛卡尔积、链式迭代 |
| `math` | `gcd`, `lcm`, `sqrt`, `ceil`, `floor`, `factorial` | 数学函数 |
| `functools` | `lru_cache`, `reduce`, `cmp_to_key` | 缓存装饰器、归约、比较转键函数 |
| `bisect` | `bisect_left`, `bisect_right`, `insort` | 二分查找与插入 |
| `heapq` | `heappush`, `heappop`, `heapify`, `nlargest`, `nsmallest` | 堆操作 |
| `sys` | `stdin.readline`, `stdout.write`, `setrecursionlimit` | 快速 IO 和递归深度设置 |
| `typing` | `List`, `Dict`, `Set`, `Tuple`, `Optional` | 类型提示（提高代码可读性） |

**常用 Python 惯用法**：

```python
# 列表推导式
squares = [x**2 for x in range(10)]

# 字典推导式
square_dict = {x: x**2 for x in range(5)}

# 枚举（同时获取索引和值）
for i, val in enumerate(['a', 'b', 'c']):
    print(i, val)

# 同时遍历多个列表
for a, b in zip(list1, list2):
    print(a, b)

# 带条件的列表推导
even_squares = [x**2 for x in range(20) if x % 2 == 0]

# 使用 Counter 计数
from collections import Counter
freq = Counter("hello world")
print(freq.most_common(3))  # 输出频率最高的 3 个字符

# 使用 lru_cache 加速递归
from functools import lru_cache
@lru_cache(maxsize=None)
def fib(n):
    if n < 2:
        return n
    return fib(n-1) + fib(n-2)
```

### 2.2.4 Java 必备特性

| 类/接口 | 包 | 说明 | 时间复杂度 |
|---------|-----|------|:----------:|
| `ArrayList` | `java.util` | 动态数组 | 随机访问 O(1)，尾部插入 O(1) 均摊 |
| `LinkedList` | `java.util` | 双向链表 | 头部/尾部插入 O(1)，随机访问 O(n) |
| `HashMap` | `java.util` | 哈希映射 | 插入/删除/查找 O(1) 均摊 |
| `TreeMap` | `java.util` | 有序映射（红黑树） | 插入/删除/查找 O(log n) |
| `HashSet` | `java.util` | 哈希集合 | 插入/删除/查找 O(1) 均摊 |
| `TreeSet` | `java.util` | 有序集合（红黑树） | 插入/删除/查找 O(log n) |
| `PriorityQueue` | `java.util` | 优先队列（堆） | push/pop O(log n) |
| `ArrayDeque` | `java.util` | 双端队列 | 两端插入/删除 O(1) |
| `StringBuilder` | `java.lang` | 可变字符串 | 追加 O(1) 均摊 |
| `Math` | `java.lang` | 数学函数 | 常用数学运算 |
| `BigInteger` | `java.math` | 大整数 | 高精度运算 |
| `Arrays` | `java.util` | 数组工具 | sort O(n log n)，binarySearch O(log n) |
| `Collections` | `java.util` | 集合工具 | sort、reverse、binarySearch 等 |

### 2.2.5 Rust 必备特性

| 标准库类型 | 说明 | 时间复杂度 |
|-----------|------|:----------:|
| `Vec<T>` | 动态数组 | 随机访问 O(1) |
| `HashMap<K, V>` | 哈希映射 | 插入/删除/查找 O(1) 均摊 |
| `HashSet<T>` | 哈希集合 | 插入/删除/查找 O(1) 均摊 |
| `BTreeMap<K, V>` | 有序映射 | 插入/删除/查找 O(log n) |
| `BTreeSet<T>` | 有序集合 | 插入/删除/查找 O(log n) |
| `BinaryHeap<T>` | 最大堆 | push/pop O(log n) |
| `VecDeque<T>` | 双端队列 | 两端插入/删除 O(1) 均摊 |
| `String` | UTF-8 字符串 | 可变，支持切片 |
| `Iterator` 适配器 | `map`, `filter`, `fold`, `collect` 等 | 链式数据处理 |

### 本节小结

| 关键点 | 说明 |
|--------|------|
| C++ STL 是竞赛核心 | vector、set、map、unordered_map、algorithm 等 |
| Python 标准库高效 | collections、itertools、heapq、bisect、functools |
| Java 集合框架完备 | HashMap、ArrayList、PriorityQueue、BigInteger |
| Rust 类型系统安全 | Vec、HashMap、BTreeMap、BinaryHeap |
| 掌握标准库 = 事半功倍 | 避免重复造轮子，聚焦算法逻辑 |

---

## 2.3 输入输出与调试

### 2.3.1 为什么输入输出和调试很重要？

在算法竞赛和编程面试中，**输入输出（IO）的效率和正确性**直接影响代码能否通过测试。低效的 IO 可能导致超时，不正确的调试方法会浪费大量时间。

### 2.3.2 快速 IO

#### C++ 快速 IO

```cpp
// 关闭 C++ 标准 IO 流与 C stdio 的同步，大幅提升 cin/cout 速度
#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    ios::sync_with_stdio(false);  // 关闭同步
    cin.tie(nullptr);              // 解除 cin 和 cout 的绑定
    cout.tie(nullptr);

    int n;
    cin >> n;                      // 现在 cin 的速度接近 scanf
    // 或者直接使用 scanf/printf
    // scanf("%d", &n);
    // printf("%d\n", n);
    return 0;
}
```

**注意**：关闭同步后，`cin` 和 `cout` 不能与 `scanf` 和 `printf` 混用。

#### Python 快速 IO

```python
import sys

# 快速读取一行（去除末尾换行符）
def input():
    return sys.stdin.readline().rstrip()

# 一次性读取所有输入（适合大数据量）
data = sys.stdin.buffer.read().split()
# data 是字节串列表，需要转换为整数
nums = list(map(int, data))

# 快速输出（使用 sys.stdout.write 替代 print）
def fast_output(arr):
    out = '\n'.join(map(str, arr))
    sys.stdout.write(out)

# ===== 示例：快速读取大量整数 =====
import sys

def solve():
    # 读取第一行（通常包含 n 和 m）
    n, m = map(int, sys.stdin.readline().split())
    # 读取后续 n 行
    arr = [list(map(int, sys.stdin.readline().split())) for _ in range(n)]
    # 处理逻辑...
    result = n + m
    # 快速输出
    sys.stdout.write(str(result))

if __name__ == "__main__":
    solve()
```

#### Java 快速 IO

```java
import java.io.*;
import java.util.*;

public class Main {
    static class FastReader {
        BufferedReader br;
        StringTokenizer st;

        public FastReader() {
            br = new BufferedReader(new InputStreamReader(System.in));
        }

        String next() {
            while (st == null || !st.hasMoreElements()) {
                try {
                    st = new StringTokenizer(br.readLine());
                } catch (IOException e) {
                    e.printStackTrace();
                }
            }
            return st.nextToken();
        }

        int nextInt() { return Integer.parseInt(next()); }
        long nextLong() { return Long.parseLong(next()); }
        double nextDouble() { return Double.parseDouble(next()); }
    }

    public static void main(String[] args) {
        FastReader sc = new FastReader();
        int n = sc.nextInt();
        int[] arr = new int[n];
        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }
        // 处理逻辑...
    }
}
```

#### Rust 快速 IO

```rust
use std::io::{self, BufRead};

fn main() {
    let stdin = io::stdin();
    // 一次性读取所有整数
    let nums: Vec<i64> = stdin
        .lock()
        .lines()
        .flat_map(|line| {
            line.unwrap()
                .split_whitespace()
                .map(|s| s.parse::<i64>().unwrap())
                .collect::<Vec<_>>()
        })
        .collect();
    // 处理逻辑...
}
```

### 2.3.3 文件 IO

在竞赛中，有时需要从文件读取输入并将结果写入文件：

#### C++ 文件 IO

```cpp
freopen("input.txt", "r", stdin);   // 重定向标准输入到文件
freopen("output.txt", "w", stdout); // 重定向标准输出到文件
// 之后正常使用 cin/cout 或 scanf/printf 即可
```

#### Python 文件 IO

```python
import sys
sys.stdin = open('input.txt', 'r')
sys.stdout = open('output.txt', 'w')
# 之后正常使用 input() 和 print()
```

### 2.3.4 调试技巧

#### 1. 打印调试（Logging）

```python
# Python 调试技巧

# 1. 使用 print 输出中间变量
def debug_example(arr):
    n = len(arr)
    for i in range(n):
        # 打印变量名和值，方便排查
        print(f"DEBUG: i={i}, arr[i]={arr[i]}", file=sys.stderr)
        # 将调试信息输出到 stderr，避免干扰 stdout

# 2. 使用条件断点（在关键位置打印）
def binary_search(arr, target):
    l, r = 0, len(arr) - 1
    while l <= r:
        mid = (l + r) // 2
        print(f"DEBUG: l={l}, r={r}, mid={mid}, arr[mid]={arr[mid]}", file=sys.stderr)
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            l = mid + 1
        else:
            r = mid - 1
    return -1

# 3. 使用 assert 进行断言检查
def divide(a, b):
    assert b != 0, "除数不能为零！"
    return a / b
```

#### 2. 对拍（Stress Testing）

对拍是竞赛中最常用的调试方法——用暴力算法验证高效算法的正确性：

```python
import random

# 暴力算法（保证正确，但可能较慢）
def brute_force(arr):
    # 例如：求最大子数组和——暴力枚举所有子数组
    n = len(arr)
    max_sum = float('-inf')
    for i in range(n):
        for j in range(i, n):
            max_sum = max(max_sum, sum(arr[i:j+1]))
    return max_sum

# 高效算法（待验证）
def efficient_solution(arr):
    # Kadane 算法（线性时间）
    max_sum = float('-inf')
    current_sum = 0
    for x in arr:
        current_sum = max(x, current_sum + x)
        max_sum = max(max_sum, current_sum)
    return max_sum

# 随机生成测试数据并对拍
def stress_test(trials=1000, max_size=100):
    for _ in range(trials):
        n = random.randint(1, max_size)
        arr = [random.randint(-1000, 1000) for _ in range(n)]
        bf = brute_force(arr)
        ef = efficient_solution(arr)
        if bf != ef:
            print(f"❌ 错误！数组: {arr}")
            print(f"暴力结果: {bf}, 高效结果: {ef}")
            return
    print("✅ 全部通过！")

# 运行对拍
stress_test(1000, 50)
```

#### 3. 随机数据生成

```python
# 生成随机测试数据
import random

def generate_random_array(n, min_val=-1000, max_val=1000):
    """生成包含 n 个随机整数的列表"""
    return [random.randint(min_val, max_val) for _ in range(n)]

def generate_random_tree(n):
    """生成一棵 n 个节点的随机树（边列表）"""
    edges = []
    for i in range(2, n + 1):
        parent = random.randint(1, i - 1)
        edges.append((parent, i))
    return edges

def generate_random_graph(n, m):
    """生成 n 个节点 m 条边的随机无向图"""
    edges = set()
    while len(edges) < m:
        u = random.randint(1, n)
        v = random.randint(1, n)
        if u != v:
            edges.add((min(u, v), max(u, v)))
    return list(edges)
```

#### 4. 使用调试器

现代 IDE 都提供了强大的调试器支持：

- **VS Code**：设置断点，逐步执行，观察变量值
- **PyCharm**：Python 调试器，支持条件断点、表达式求值
- **CLion**：C++ 调试器，支持内存查看、反汇编
- **IntelliJ IDEA**：Java 调试器，支持线程调试、远程调试

**调试器核心操作**：

| 操作 | 说明 |
|------|------|
| 设置断点 (F9) | 在代码行点击行号区域 |
| 单步执行 (F10) | 逐行执行代码 |
| 步入函数 (F11) | 进入函数内部调试 |
| 步出函数 (Shift+F11) | 跳出当前函数 |
| 观察变量 | 在调试面板中查看变量当前值 |
| 条件断点 | 满足特定条件时才触发断点 |

### 2.3.5 测试框架

#### Python unittest

```python
import unittest

def max_subarray_sum(arr):
    """Kadane 算法求最大子数组和"""
    if not arr:
        return 0
    max_sum = current_sum = arr[0]
    for x in arr[1:]:
        current_sum = max(x, current_sum + x)
        max_sum = max(max_sum, current_sum)
    return max_sum

class TestMaxSubarray(unittest.TestCase):
    def test_basic(self):
        self.assertEqual(max_subarray_sum([-2, 1, -3, 4, -1, 2, 1, -5, 4]), 6)

    def test_all_negative(self):
        self.assertEqual(max_subarray_sum([-5, -2, -3, -1]), -1)

    def test_all_positive(self):
        self.assertEqual(max_subarray_sum([1, 2, 3, 4, 5]), 15)

    def test_single_element(self):
        self.assertEqual(max_subarray_sum([7]), 7)
        self.assertEqual(max_subarray_sum([-3]), -3)

    def test_empty_array(self):
        self.assertEqual(max_subarray_sum([]), 0)

if __name__ == '__main__':
    unittest.main()
```

#### Python pytest（更简洁）

```python
# 安装: pip install pytest
# 运行: pytest 文件名.py

def max_subarray_sum(arr):
    if not arr:
        return 0
    max_sum = current_sum = arr[0]
    for x in arr[1:]:
        current_sum = max(x, current_sum + x)
        max_sum = max(max_sum, current_sum)
    return max_sum

def test_basic():
    assert max_subarray_sum([-2, 1, -3, 4, -1, 2, 1, -5, 4]) == 6

def test_all_negative():
    assert max_subarray_sum([-5, -2, -3, -1]) == -1

def test_all_positive():
    assert max_subarray_sum([1, 2, 3, 4, 5]) == 15

def test_edge_cases():
    assert max_subarray_sum([]) == 0
    assert max_subarray_sum([7]) == 7
```

### 本节小结

| 关键点 | 说明 |
|--------|------|
| C++ 快速 IO | `ios::sync_with_stdio(false)` + `cin.tie(nullptr)` |
| Python 快速 IO | `sys.stdin.buffer.read()` + `sys.stdout.write()` |
| Java 快速 IO | 自定义 `FastReader` 类包装 `BufferedReader` |
| 文件 IO | C++ 用 `freopen`，Python 用 `sys.stdin = open(...)` |
| 对拍测试 | 暴力算法验证高效算法的正确性 |
| 随机数据生成 | 自动生成测试用例覆盖边界情况 |
| 调试器使用 | 断点、单步执行、变量观察 |
| 单元测试 | unittest / pytest 确保代码正确性 |

---

## 2.4 时间复杂度分析

### 2.4.1 什么是时间复杂度？

在计算机科学中，**时间复杂度**（Time Complexity）定性描述了一个算法运行所需要的时间，它是输入规模 $n$ 的函数。时间复杂度分析帮助我们预测算法在大规模输入下的表现，而不必实际运行代码。

时间复杂度通常使用**大 O 符号**（Big O notation）来表示。大 O 符号描述的是算法的**渐近上界**（asymptotic upper bound），即当输入规模 $n$ 趋近于无穷大时，算法运行时间的增长趋势。

> 大 O 符号由德国数论学家保罗·巴赫曼在 1892 年首先引入，由爱德蒙·兰道推广，因此有时也称为**兰道符号**（Landau symbols）。

### 2.4.2 大 O 符号的数学定义

设 $f(n)$ 和 $g(n)$ 是定义在正整数集上的函数。若存在正常数 $c$ 和 $n_0$，使得对所有 $n \ge n_0$，都有 $0 \le f(n) \le c \cdot g(n)$，则称 $f(n) = O(g(n))$。

**直观理解**：大 O 给出了算法运行时间的**最坏情况上界**。$f(n) = O(n^2)$ 意味着当 $n$ 足够大时，$f(n)$ 的增长速度不会超过 $n^2$ 的常数倍。

### 2.4.3 常见时间复杂度

| 名称 | 大 O 表示 | 直观含义 | 典型算法 |
|------|:---------:|---------|---------|
| 常数时间 | $O(1)$ | 运行时间与输入规模无关 | 数组随机访问、哈希表查找 |
| 对数时间 | $O(\log n)$ | 每步将问题规模减半 | 二分查找、平衡树操作 |
| 线性时间 | $O(n)$ | 运行时间与输入规模成正比 | 线性扫描、前缀和 |
| 线性对数时间 | $O(n \log n)$ | 线性与对数的组合 | 归并排序、快速排序（平均） |
| 平方时间 | $O(n^2)$ | 双层循环 | 冒泡排序、朴素 DP |
| 立方时间 | $O(n^3)$ | 三层循环 | Floyd-Warshall 最短路 |
| 指数时间 | $O(2^n)$ | 规模每增 1，时间翻倍 | 子集枚举、暴力搜索 |
| 阶乘时间 | $O(n!)$ | 全排列穷举 | 旅行商问题暴力解 |

### 2.4.4 如何分析时间复杂度？

#### 基本规则

1. **顺序结构**：总复杂度等于各步骤复杂度之和，取最大项
2. **循环结构**：循环次数 × 循环体复杂度
3. **嵌套循环**：各层循环次数相乘
4. **递归结构**：通过递推关系求解（如主定理）

#### 分析示例

```python
# 示例 1：常数时间 O(1)
def constant_time(arr):
    """无论数组多大，都只执行固定操作"""
    if len(arr) > 0:
        return arr[0]  # 只访问第一个元素
    return -1

# 示例 2：线性时间 O(n)
def linear_time(arr):
    """遍历数组一次"""
    total = 0
    for x in arr:          # 循环 n 次
        total += x         # 每次 O(1)
    return total
# 总复杂度：n × O(1) = O(n)

# 示例 3：平方时间 O(n²)
def quadratic_time(arr):
    """双层嵌套循环"""
    n = len(arr)
    result = []
    for i in range(n):          # 外层 n 次
        for j in range(i, n):   # 内层 (n-i) 次，平均 n/2 次
            result.append(arr[i] + arr[j])  # O(1)
    return result
# 总复杂度：n × (n/2) × O(1) = O(n²)

# 示例 4：对数时间 O(log n)
def logarithmic_time(arr, target):
    """二分查找——每次将搜索范围减半"""
    left, right = 0, len(arr) - 1
    while left <= right:        # 每次循环将范围减半
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1
# 总复杂度：经过 k 次循环后，n / 2^k ≤ 1，即 k = log₂(n)，所以 O(log n)

# 示例 5：线性对数时间 O(n log n)
def linearithmic_time(arr):
    """归并排序"""
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    left = linearithmic_time(arr[:mid])   # 递归 T(n/2)
    right = linearithmic_time(arr[mid:])  # 递归 T(n/2)
    return merge(left, right)             # 合并 O(n)
# 递推公式：T(n) = 2T(n/2) + O(n)
# 根据主定理：T(n) = O(n log n)

# 示例 6：指数时间 O(2ⁿ)
def exponential_time(n):
    """斐波那契数列（朴素递归）"""
    if n <= 1:
        return n
    return exponential_time(n-1) + exponential_time(n-2)
# 递推公式：T(n) = T(n-1) + T(n-2) + O(1)
# 解为：T(n) = O(2ⁿ)
```

### 2.4.5 时间复杂度分析示例：综合对比

```python
import time
import random

# 生成测试数据
n = 10000
arr = list(range(n))
target = n - 1

# O(1) 示例
def constant_access(arr):
    return arr[-1]  # 总是最后一个元素，直接索引

# O(log n) 示例
def binary_search(arr, target):
    l, r = 0, len(arr) - 1
    while l <= r:
        mid = (l + r) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            l = mid + 1
        else:
            r = mid - 1
    return -1

# O(n) 示例
def linear_search(arr, target):
    for i, x in enumerate(arr):
        if x == target:
            return i
    return -1

# O(n²) 示例
def bubble_sort(arr):
    n = len(arr)
    arr = arr[:]  # 复制，避免修改原数组
    for i in range(n):
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr

# 计时对比
print(f"数据规模: n = {n}")
print("-" * 40)

start = time.time()
constant_access(arr)
print(f"O(1) 常数时间:     {time.time() - start:.6f} 秒")

start = time.time()
binary_search(arr, target)
print(f"O(log n) 对数时间: {time.time() - start:.6f} 秒")

start = time.time()
linear_search(arr, target)
print(f"O(n) 线性时间:     {time.time() - start:.6f} 秒")

# 注意：O(n²) 只对 n=1000 运行，否则太慢
small_arr = list(range(1000))
start = time.time()
bubble_sort(small_arr)
print(f"O(n²) 平方时间:    {time.time() - start:.6f} 秒 (n=1000)")
```

### 2.4.6 空间复杂度

空间复杂度（Space Complexity）定性地描述算法运行所需要的存储空间大小，同样使用大 O 符号表示。

**常见空间复杂度**：

| 空间复杂度 | 含义 | 示例 |
|:----------:|------|------|
| $O(1)$ | 仅使用固定数量的额外空间 | 原地交换、累加求和 |
| $O(n)$ | 需要线性的额外空间 | 复制数组、递归栈空间 |
| $O(n^2)$ | 需要平方级的额外空间 | 二维矩阵、邻接矩阵 |

```python
# 示例 1：O(1) 额外空间
def sum_array(arr):
    """只使用一个变量 total，空间 O(1)"""
    total = 0
    for x in arr:
        total += x
    return total

# 示例 2：O(n) 额外空间
def copy_array(arr):
    """创建了一个新数组，空间 O(n)"""
    new_arr = arr[:]  # 复制整个数组
    return new_arr

# 示例 3：O(n) 递归栈空间
def factorial_recursive(n):
    """递归深度为 n，调用栈占用 O(n) 空间"""
    if n <= 1:
        return 1
    return n * factorial_recursive(n - 1)

# 示例 4：O(n²) 额外空间
def create_adjacency_matrix(n, edges):
    """创建 n×n 的邻接矩阵，空间 O(n²)"""
    matrix = [[0] * n for _ in range(n)]
    for u, v in edges:
        matrix[u][v] = 1
        matrix[v][u] = 1
    return matrix
```

### 2.4.7 常见复杂度对应的数据规模

在竞赛和面试中，根据输入规模可以快速判断算法应该达到的复杂度级别：

| 数据规模 $n$ | 可接受的复杂度 | 典型算法 |
|:-----------:|:-------------:|---------|
| $n \le 12$ | $O(n!)$ 或 $O(2^n)$ | 暴力枚举全排列、状态压缩 |
| $n \le 20$ | $O(2^n)$ | 状态压缩 DP、子集枚举 |
| $n \le 100$ | $O(n^3)$ | Floyd-Warshall、三重循环 |
| $n \le 1000$ | $O(n^2)$ | 冒泡排序、朴素 DP、双层循环 |
| $n \le 10^5$ | $O(n \log n)$ | 排序算法、Dijkstra、分治算法 |
| $n \le 10^7$ | $O(n)$ | 线性扫描、前缀和、线性 DP |
| $n \le 10^{12}$ | $O(\log n)$ | 二分查找、GCD、快速幂 |
| $n \le 10^{18}$ | $O(1)$ | 数学公式直接求解 |

### 2.4.8 主定理（Master Theorem）

对于递归算法 $T(n) = aT(n/b) + f(n)$，其中 $a \ge 1, b > 1$，$f(n)$ 是渐近正函数：

| 情况 | 条件 | 解 |
|:---:|:----:|:----:|
| 1 | $f(n) = O(n^{\log_b a - \epsilon})$，$\epsilon > 0$ | $T(n) = \Theta(n^{\log_b a})$ |
| 2 | $f(n) = \Theta(n^{\log_b a})$ | $T(n) = \Theta(n^{\log_b a} \log n)$ |
| 3 | $f(n) = \Omega(n^{\log_b a + \epsilon})$，且 $af(n/b) \le cf(n)$ | $T(n) = \Theta(f(n))$ |

**示例**：
- 归并排序：$T(n) = 2T(n/2) + O(n)$，$a=2, b=2, \log_b a = 1$，$f(n) = \Theta(n)$ → 情况 2，$T(n) = \Theta(n \log n)$
- 二分查找：$T(n) = T(n/2) + O(1)$，$a=1, b=2, \log_b a = 0$，$f(n) = O(1) = \Theta(n^0)$ → 情况 2，$T(n) = \Theta(\log n)$

### 本节小结

| 关键点 | 说明 |
|--------|------|
| 大 O 符号 | 描述算法运行时间的渐近上界 |
| 常见复杂度 | $O(1) < O(\log n) < O(n) < O(n \log n) < O(n^2) < O(2^n) < O(n!)$ |
| 分析规则 | 顺序取最大，循环相乘，嵌套相乘，递归用主定理 |
| 空间复杂度 | 同样使用大 O 表示，关注额外空间开销 |
| 数据规模与复杂度 | $n \le 10^5$ 需 $O(n \log n)$ 或更优，$n \le 10^7$ 需 $O(n)$ 或更优 |
| 主定理 | 解决 $T(n) = aT(n/b) + f(n)$ 形式的递归复杂度 |

---

## 参考资源

以下维基百科文章提供了关于算法复杂度分析的权威参考资料：

- [大O符号 - Wikipedia 文章 2974574](https://zh.wikipedia.org/wiki/%E5%A4%A7O%E7%AC%A6%E5%8F%B7)
- [时间复杂度 - Wikipedia 文章 3479880](https://zh.wikipedia.org/wiki/%E6%97%B6%E9%97%B4%E5%A4%8D%E6%9D%82%E5%BA%A6)
- [空间复杂度 - Wikipedia 文章 4040642](https://zh.wikipedia.org/wiki/%E7%A9%BA%E9%97%B4%E5%A4%8D%E6%9D%82%E5%BA%A6)
- [算法分析 - Wikipedia 文章 4065733](https://zh.wikipedia.org/wiki/%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90)
- [数据结构 - Wikipedia 文章 3420564](https://zh.wikipedia.org/wiki/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84)

---

> **说明**：本文档结合了中文维基百科相关内容整理而成。