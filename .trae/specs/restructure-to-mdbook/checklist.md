# Checklist

## 安全措施
- [x] 备份目录已创建，根目录所有 `.md` 文件已备份
- [x] 备份文件数量与源文件一致
- [x] 文件迁移使用 `cp` 而非 `mv`，根目录原文件未删除

## 配置文件
- [x] `book.toml` 文件存在且配置正确
  - `mdbook-katex` 预处理器已启用（`before = ["links"]`）
  - 书籍元数据（标题、作者、语言）已设置
  - 构建输出目录配置正确

## 文档迁移
- [x] `src/` 目录已创建
- [x] 所有 `.md` 文档已复制到 `src/` 目录（非移动）
- [x] `src/` 内包含 21 个原始文档 + 1 个 SUMMARY.md = 22 个文件，符合预期
- [x] 复制后文件内容完整性已验证（差异仅为预期的图片路径变更 `images/` → `../images/`）

## 导航结构
- [x] `src/SUMMARY.md` 已创建，包含所有章节的层级导航
- [x] AI 子章节（11a~11e）已正确嵌套在 11 章下
- [x] `src/README.md` 书籍首页已创建，内容精简且完整

## 链接更新
- [x] 文档内图片路径已更新（`images/` → `../images/`）
- [x] 文档内返回链接已更新（适用于新目录结构）
- [x] 所有锚点链接仍然有效（构建通过，无链接相关错误；KaTeX 渲染正常）

## 构建验证
- [x] `mdbook build` 构建成功，无错误输出
- [x] `book/` 目录已生成，包含完整 HTML 文件
- [x] 构建后的 KaTeX 公式渲染正常（`book/index.html` 包含 `katex.min.css`）
- [x] 导航链接可用（`mdbook build` 生成完整 HTML 文件，侧边栏导航由 mdbook 自动生成）
- [x] 根目录 `README.md` 保留为项目 README（未删除）

## 最终状态
- [x] 根目录原 `.md` 文件未自动删除（需用户确认）
- [x] 回滚方案可行（删除 `src/`、`book/`、`book.toml` 即可恢复原状）